import React from 'react';
import { DatasetCreationAttributes, MetaResponse } from './common';
export type RasterTilesetMeta = {
    name: string;
    metadataUrl: string;
    rasterTileServerUrls?: string[];
};
export declare function getDatasetAttributesFromRasterTile({ name, metadataUrl, rasterTileServerUrls }: RasterTilesetMeta): DatasetCreationAttributes;
type RasterTileFormProps = {
    setResponse: (response: MetaResponse) => void;
};
declare const RasterTileForm: React.FC<RasterTileFormProps>;
export default RasterTileForm;
