import React, { Component } from 'react';
import { BaseProps } from './base';
declare class BaseMap extends Component<Partial<BaseProps>> {
    static defaultProps: {
        height: string;
        predefinedClassName: string;
        viewBox: string;
        totalColor: number;
    };
    render(): React.JSX.Element;
}
export default BaseMap;
